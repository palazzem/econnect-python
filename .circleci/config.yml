version: 2
jobs:
  build:
    docker:
      - image: circleci/python:3.7.1

    steps:
      - checkout
      - run:
          name: install dependencies
          command: |
            sudo pip install pipenv tox
      - run:
          name: run tests
          command: |
            tox
